<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".util.CaptainApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/captain"
        android:label="@string/app_name"
        android:roundIcon="@drawable/captain"
        android:supportsRtl="true"
        android:theme="@style/Theme.ATMODRIVECAPTAIN"
        tools:targetApi="31">
        <activity
            android:name=".futures.auth.presentation.AuthActivity"
            android:exported="false" />
        <activity
            android:name=".futures.splash.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>